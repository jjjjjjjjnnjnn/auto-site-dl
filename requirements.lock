# 锁定版本(2026-09-25 实测环境). 完整 hash 锁定需维护者在联网机器执行:
#   pip hash playwright==1.60.0 requests==2.33.0 ... >> requirements.lock
# 然后安装用: pip install --require-hashes -r requirements.lock
playwright==1.60.0
requests==2.33.0
curl-cffi==0.16.3  # 可选: TLS 指纹伪装, 缺失自动回落 requests
camoufox==0.5.6  # 可选: C++ 层浏览器指纹, 另需 python -m camoufox fetch
